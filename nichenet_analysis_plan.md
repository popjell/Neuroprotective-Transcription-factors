look at ligand-receptor interactions for specific gene lists. compare to bg (options; all expressed, all detected genes (either rc>0 or like at least rc >10) or all upregulated; or specific to certain cell types or intersected upregulated)


get the ligand-matrix R object.
intersect the expressed genes (background genes) common to MN/RGC (or wtv background u use)
Make sure every receptor is in the dataset.

Run AUPR analysis to compare specific functional gene list (metabolism/cell death) vs background
or nichenet predict ligand activities

later, see if the ligand targets have increased fold change